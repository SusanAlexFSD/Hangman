# Hangman
A simple interactive Hangman game built with React and styled with CSS. The game selects a random word, tracks player guesses, and updates the UI dynamically as the player reveals letters or makes mistakes.
